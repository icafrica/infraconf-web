# Cloudflare Pages – static Astro site routing
# Astro SSG generates individual HTML files per route, so direct file access works.
# Catch-all 404 fallback for any URL that doesn't match a real file.
/* /404.html 404
