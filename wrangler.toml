# Cloudflare Pages configuration
# This file is used by Cloudflare Pages to configure the build environment.

name = "infraconf-africa"
pages_build_output_dir = "dist"

[build]
command = "npm run build"

[build.environment]
NODE_VERSION = "22"
